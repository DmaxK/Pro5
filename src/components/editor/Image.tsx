import React from 'react'
import { Vector3 } from 'three'
import { PivotControls, Center } from '@react-three/drei'

const Image: React.FC<{spawnPosition: THREE.Vector3, pivotEnabled: boolean}> = ({spawnPosition, pivotEnabled}) => {

  return (
    <div>Image</div>
  )
}

export default Image